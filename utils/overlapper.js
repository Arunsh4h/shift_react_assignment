// will return true if shidt is overlaped
const checkOverlap = allShifts => {
  let bookedShifts = [];
  /*
        [
            5.5,
            8.5
        ]

        7.5
    */
  let availableShifts = [];

  allShifts.forEach(shift => {
    const startTime = shift.startTime;
    const endTime = shift.endTime;
  });
};
